apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: grade-submission-portal-autoscaler
  namespace: grade-submission
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    # applicable to all pods with name app.kubernetes.io/instance=grade-submission-portal
    kind: Deployment
    name: grade-submission-portal
  minReplicas: 1
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 50
